export enum EventBadgeRequirementType {
  POSTS_COUNT = 'POSTS_COUNT',           // Kullanıcının attığı post sayısı
  LIKES_RECEIVED = 'LIKES_RECEIVED',     // Kullanıcının postlarına gelen beğeni sayısı (ALAN)
  TOTAL_SCORE = 'TOTAL_SCORE'
}
