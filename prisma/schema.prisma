generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                      String                  @id @default(uuid()) @db.Uuid
  email                   String?                 @unique
  passwordHash            String?                 @map("password_hash")
  status                  String?
  emailVerified           Boolean                 @default(false) @map("email_verified")
  createdAt               DateTime                @default(now()) @map("created_at")
  updatedAt               DateTime                @updatedAt @map("updated_at")
  adminLogs               AdminLog[]
  aiExperienceSplits      AiExperienceSplit[]     @relation("UserAiExperienceSplits")
  brandSurveyAnswers      BrandSurveyAnswer[]
  bridgeFollowers         BridgeFollower[]
  bridgeLeaderboards      BridgeLeaderboard[]
  bridgePosts             BridgePost[]
  bridgeRewards           BridgeReward[]
  bridgeUserStats         BridgeUserStats[]
  choiceComments          ChoiceComment[]
  contentCollections      ContentCollection[]
  contentCommentVotes     ContentCommentVote[]
  contentComments         ContentComment[]
  contentFavorites        ContentFavorite[]
  contentLikes            ContentLike[]
  contentPostViews        ContentPostView[]
  contentPosts            ContentPost[]
  contentRatings          ContentRating[]
  contentShares           ContentShare[]
  dmMessages              DMMessage[]
  dmRequestsSent          DMRequest[]             @relation("DMRequestSender")
  dmRequestsReceived      DMRequest[]             @relation("DMRequestReceiver")
  dmSupportSessions       DMSupportSession[]
  dmThreadsAsUserOne      DMThread[]              @relation("DMThreadUserOne")
  dmThreadsAsUserTwo      DMThread[]              @relation("DMThreadUserTwo")
  emailVerificationCodes  EmailVerificationCode[]
  expertAnswers           ExpertAnswer[]
  expertRequests          ExpertRequest[]
  feeds                   Feed[]
  inventories             Inventory[]
  loginAttempts           LoginAttempt[]
  lootboxes               Lootbox[]
  manualReviewFlags       ManualReviewFlag[]
  moderatorActions        ModerationAction[]      @relation("ModeratorActions")
  moderatedActions        ModerationAction[]      @relation("ModeratedActions")
  nftClaims               NFTClaim[]
  nftMarketListings       NFTMarketListing[]
  nftTransactionsSent     NFTTransaction[]        @relation("NFTSender")
  nftTransactionsReceived NFTTransaction[]        @relation("NFTReceiver")
  nftOwned                NFT[]                   @relation("NFTOwner")
  passwordResetTokens     PasswordResetToken[]
  reviewedSuggestions     ProductSuggestion[]     @relation("ProductSuggestionReviewer")
  productSuggestions      ProductSuggestion[]
  profile                 Profile?
  rewardClaims            RewardClaim[]
  scenarioChoices         ScenarioChoice[]
  supportRequestReports   SupportRequestReport[]  @relation("SupportRequestReportReporter")
  userReportsAsReporter   UserReport[]            @relation("UserReportReporter")
  userReportsAsReported   UserReport[]            @relation("UserReportReported")
  sentTips                TipsTokenTransfer[]     @relation("SentTips")
  receivedTips            TipsTokenTransfer[]     @relation("ReceivedTips")
  trustRelationsAsTrusted TrustRelation[]         @relation("TrustedUserRelation")
  trustRelationsAsTarget  TrustRelation[]         @relation("TrusterRelation")
  userAchievements        UserAchievement[]
  avatars                 UserAvatar[]
  userBadges              UserBadge[]
  userBlocksAsBlocked     UserBlock[]             @relation("BlockedUserRelation")
  userBlocksAsBlocker     UserBlock[]             @relation("BlockerRelation")
  collections             UserCollection[]
  feedPreferences         UserFeedPreferences?
  userKycRecords          UserKycRecord[]
  userMutesAsMuted        UserMute[]              @relation("MutedUserRelation")
  userMutesAsMuter        UserMute[]              @relation("MuterRelation")
  roles                   UserRole[]
  settings                UserSettings?
  titles                  UserTitle[]
  trustScores             UserTrustScore[]
  wallets                 Wallet[]
  wishboxRewards          WishboxReward[]
  wishboxStats            WishboxStats[]
  notifications           Notification[]
  pushTokens              PushToken[]

  @@map("users")
}

model Profile {
  id              String    @id @default(uuid()) @db.Uuid
  userId          String    @unique @map("user_id") @db.Uuid
  displayName     String?   @map("display_name")
  userName        String?   @unique @map("user_name")
  bio             String?
  bannerUrl       String?   @map("banner_url")
  country         String?
  birthDate       DateTime? @map("birth_date") @db.Date
  createdAt       DateTime  @default(now()) @map("created_at")
  updatedAt       DateTime  @updatedAt @map("updated_at")
  postsCount      Int       @default(0) @map("posts_count")
  trustCount      Int       @default(0) @map("trust_count")
  trusterCount    Int       @default(0) @map("truster_count")
  unseenFeedCount Int       @default(0) @map("unseen_feed_count")
  cosmeticBadgeId String?   @map("cosmetic_badge_id") @db.Uuid
  cosmeticBadge   Badge?    @relation("ProfileCosmeticBadge", fields: [cosmeticBadgeId], references: [id])
  user            User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("profiles")
}

model UserSettings {
  id                   String          @id @default(uuid()) @db.Uuid
  userId               String          @unique @map("user_id") @db.Uuid
  themeId              String?         @map("theme_id") @db.Uuid
  receiveNotifications Boolean?        @map("receive_notifications")
  visibility           UserVisibility?

  // Notification Channel Preferences
  notificationEmailEnabled Boolean? @default(true) @map("notification_email_enabled")
  notificationPushEnabled  Boolean? @default(true) @map("notification_push_enabled")
  notificationInAppEnabled Boolean? @default(true) @map("notification_in_app_enabled")

  // Notification Type Preferences
  trustNotifications      Boolean @default(true) @map("trust_notifications")
  supportNotifications    Boolean @default(true) @map("support_notifications")
  messageNotifications    Boolean @default(true) @map("message_notifications")
  collectionNotifications Boolean @default(true) @map("collection_notifications")
  postNotifications       Boolean @default(true) @map("post_notifications")

  createdAt DateTime   @default(now()) @map("created_at")
  updatedAt DateTime   @updatedAt @map("updated_at")
  theme     UserTheme? @relation(fields: [themeId], references: [id])
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("user_settings")
}

model TrustRelation {
  id            String   @id @default(uuid()) @db.Uuid
  trusterId     String   @map("truster_id") @db.Uuid
  trustedUserId String   @map("trusted_user_id") @db.Uuid
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt @map("updated_at")
  trustedUser   User     @relation("TrustedUserRelation", fields: [trustedUserId], references: [id], onDelete: Cascade)
  truster       User     @relation("TrusterRelation", fields: [trusterId], references: [id], onDelete: Cascade)

  @@unique([trusterId, trustedUserId])
  @@index([trusterId])
  @@index([trustedUserId])
  @@map("trust_relations")
}

model UserBlock {
  id            String   @id @default(uuid()) @db.Uuid
  blockerId     String   @map("blocker_id") @db.Uuid
  blockedUserId String   @map("blocked_user_id") @db.Uuid
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt @map("updated_at")
  blockedUser   User     @relation("BlockedUserRelation", fields: [blockedUserId], references: [id], onDelete: Cascade)
  blocker       User     @relation("BlockerRelation", fields: [blockerId], references: [id], onDelete: Cascade)

  @@unique([blockerId, blockedUserId])
  @@map("user_blocks")
}

model UserMute {
  id          String   @id @default(uuid()) @db.Uuid
  muterId     String   @map("muter_id") @db.Uuid
  mutedUserId String   @map("muted_user_id") @db.Uuid
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  mutedUser   User     @relation("MutedUserRelation", fields: [mutedUserId], references: [id], onDelete: Cascade)
  muter       User     @relation("MuterRelation", fields: [muterId], references: [id], onDelete: Cascade)

  @@unique([muterId, mutedUserId])
  @@map("user_mutes")
}

model UserTrustScore {
  id           String   @id @default(uuid()) @db.Uuid
  userId       String   @map("user_id") @db.Uuid
  score        Float
  reason       String?
  calculatedAt DateTime @default(now()) @map("calculated_at")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("user_trust_scores")
}

model UserRole {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  role      String
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([role])
  @@map("user_roles")
}

model UserFeedPreferences {
  id                    String   @id @default(uuid()) @db.Uuid
  userId                String   @unique @map("user_id") @db.Uuid
  preferredCategories   String?  @map("preferred_categories")
  preferredContentTypes String?  @map("preferred_content_types")
  language              String?
  createdAt             DateTime @default(now()) @map("created_at")
  updatedAt             DateTime @updatedAt @map("updated_at")
  user                  User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("user_feed_preferences")
}

model UserAvatar {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  imageUrl  String   @map("image_url")
  isActive  Boolean  @map("is_active")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([userId, isActive])
  @@map("user_avatars")
}

model UserKycRecord {
  id                String          @id @default(uuid()) @db.Uuid
  userId            String          @map("user_id") @db.Uuid
  sumsubApplicantId String          @unique @map("sumsub_applicant_id")
  reviewStatus      KycReviewStatus @default(INIT) @map("review_status")
  reviewResult      KycReviewResult @default(NULL) @map("review_result")
  reviewReason      String?         @map("review_reason")
  kycLevel          String?         @map("kyc_level")
  provider          KycProvider     @default(SUMSUB)
  createdAt         DateTime        @default(now()) @map("created_at")
  updatedAt         DateTime        @updatedAt @map("updated_at")
  lastUpdatedAt     DateTime        @default(now()) @map("last_updated_at")
  lastSyncedAt      DateTime?       @map("last_synced_at")
  user              User            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([reviewStatus])
  @@map("user_kyc_records")
}

model UserTheme {
  id           String         @id @default(uuid()) @db.Uuid
  name         String
  description  String?
  createdAt    DateTime       @default(now()) @map("created_at")
  updatedAt    DateTime       @updatedAt @map("updated_at")
  userSettings UserSettings[]

  @@map("user_themes")
}

model UserTitle {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  title     String
  earnedAt  DateTime @default(now()) @map("earned_at")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("user_titles")
}

model UserCollection {
  id          String   @id @default(uuid()) @db.Uuid
  userId      String   @map("user_id") @db.Uuid
  name        String
  description String?
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("user_collections")
}

model LoginAttempt {
  id          String             @id @default(uuid()) @db.Uuid
  userId      String?            @map("user_id") @db.Uuid
  ipAddress   String             @map("ip_address")
  userAgent   String             @map("user_agent")
  status      LoginAttemptStatus
  attemptedAt DateTime           @default(now()) @map("attempted_at")
  user        User?              @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("login_attempts")
}

model PasswordResetToken {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  token     String   @unique
  isUsed    Boolean  @default(false) @map("is_used")
  expiresAt DateTime @map("expires_at")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("password_reset_tokens")
}

model EmailVerificationCode {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  email     String
  code      String   @db.VarChar(6)
  isUsed    Boolean  @default(false) @map("is_used")
  expiresAt DateTime @map("expires_at")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, code])
  @@index([email, code])
  @@map("email_verification_codes")
}

model Category {
  id          String   @id
  parentId    String?  @map("parent_id")
  name        String
  description String?
  thumbnail   String?  @map("thumbnail")
  mpath       String?  @map("mpath")
  handle      String?  @map("handle")
  rank        Int?     @map("rank")
  metadata    Json?    @default("{}") @map("metadata")
  isActive    Boolean? @default(true) @map("is_active")
  level       Int?     @default(0)
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  parent       Category?     @relation("CategoryChildren", fields: [parentId], references: [id], onDelete: Cascade)
  children     Category[]    @relation("CategoryChildren")
  contentPosts ContentPost[]
  products     Product[]

  @@index([parentId])
  @@map("categories")
}

model MainCategory {
  id            String        @id @default(uuid()) @db.Uuid
  name          String
  description   String?
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")
  imageUrl      String?       @map("image_url")
  contentPosts  ContentPost[]
  subCategories SubCategory[]

  @@map("main_categories")
}

model SubCategory {
  id             String         @id @default(uuid()) @db.Uuid
  mainCategoryId String         @map("main_category_id") @db.Uuid
  name           String
  description    String?
  createdAt      DateTime       @default(now()) @map("created_at")
  updatedAt      DateTime       @updatedAt @map("updated_at")
  imageUrl       String?        @map("image_url")
  contentPosts   ContentPost[]
  productGroups  ProductGroup[]
  mainCategory   MainCategory   @relation(fields: [mainCategoryId], references: [id], onDelete: Cascade)

  @@map("sub_categories")
}

model ProductGroup {
  id            String        @id @default(uuid()) @db.Uuid
  subCategoryId String        @map("sub_category_id") @db.Uuid
  name          String
  description   String?
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")
  imageUrl      String?       @map("image_url")
  contentPosts  ContentPost[]
  subCategory   SubCategory   @relation(fields: [subCategoryId], references: [id], onDelete: Cascade)
  products      Product[]

  @@map("product_groups")
}

model Product {
  id          String   @id
  name        String
  description String?
  groupId     String?  @map("group_id") @db.Uuid
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  imageUrl    String?  @map("image_url")

  subName            String?             @map("sub_name")
  aiExperienceSplits AiExperienceSplit[] @relation("ProductAiExperienceSplits")
  contentPosts       ContentPost[]
  inventories        Inventory[]
  comparisonProduct1 PostComparison[]    @relation("ComparisonProduct1")
  comparisonProduct2 PostComparison[]    @relation("ComparisonProduct2")
  questionPosts      PostQuestion[]
  group              ProductGroup?       @relation(fields: [groupId], references: [id])
  category           Category?           @relation(fields: [categoryId], references: [id])
  categoryId         String?
  metadata           Json?               @default("{}") @map("metadata")
  thumbnail          String?             @map("thumbnail")
  brand              Brand?              @relation(fields: [brandId], references: [externalId])
  brandId            String?             @map("brand_id")

  @@map("products")
}

model ProductSuggestion {
  id             String                  @id @default(uuid()) @db.Uuid
  userId         String                  @map("user_id") @db.Uuid
  suggestedName  String                  @map("suggested_name")
  suggestedBrand String?                 @map("suggested_brand")
  description    String?
  reason         String?
  status         ProductSuggestionStatus @default(PENDING)
  createdAt      DateTime                @default(now()) @map("created_at")
  updatedAt      DateTime                @updatedAt @map("updated_at")
  reviewedAt     DateTime?               @map("reviewed_at")
  reviewedBy     String?                 @map("reviewed_by") @db.Uuid
  reviewer       User?                   @relation("ProductSuggestionReviewer", fields: [reviewedBy], references: [id])
  user           User                    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("product_suggestions")
}

model Inventory {
  id                   String              @id @default(uuid()) @db.Uuid
  userId               String              @map("user_id") @db.Uuid
  productId            String              @map("product_id")
  hasOwned             Boolean             @map("has_owned")
  experienceSummary    String?             @map("experience_summary")
  createdAt            DateTime            @default(now()) @map("created_at")
  updatedAt            DateTime            @updatedAt @map("updated_at")
  experienceDurationId String?             @map("experience_duration_id") @db.Uuid
  experienceLocationId String?             @map("experience_location_id") @db.Uuid
  experiencePurposeId  String?             @map("experience_purpose_id") @db.Uuid
  experienceSnippetId  String?             @map("experience_snippet_id") @db.Uuid
  experienceDuration   ExperienceDuration? @relation(fields: [experienceDurationId], references: [id])
  experienceLocation   ExperienceLocation? @relation(fields: [experienceLocationId], references: [id])
  experiencePurpose    ExperiencePurpose?  @relation(fields: [experiencePurposeId], references: [id])
  experienceSnippet    AiExperienceSplit?  @relation(fields: [experienceSnippetId], references: [id])
  product              Product             @relation(fields: [productId], references: [id], onDelete: Cascade)
  user                 User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  media                InventoryMedia[]
  productExperiences   ProductExperience[]

  @@unique([userId, productId])
  @@index([userId])
  @@index([productId])
  @@index([userId, hasOwned])
  @@index([experienceSnippetId])
  @@index([experienceDurationId])
  @@index([experienceLocationId])
  @@index([experiencePurposeId])
  @@map("inventories")
}

model ProductExperience {
  id             String    @id @default(uuid()) @db.Uuid
  inventoryId    String    @map("inventory_id") @db.Uuid
  title          String
  experienceText String    @map("experience_text")
  createdAt      DateTime  @default(now()) @map("created_at")
  updatedAt      DateTime  @updatedAt @map("updated_at")
  inventory      Inventory @relation(fields: [inventoryId], references: [id], onDelete: Cascade)

  @@index([inventoryId])
  @@map("product_experiences")
}

model InventoryMedia {
  id          String    @id @default(uuid()) @db.Uuid
  inventoryId String    @map("inventory_id") @db.Uuid
  mediaUrl    String    @map("media_url")
  uploadedAt  DateTime  @default(now()) @map("uploaded_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")
  inventory   Inventory @relation(fields: [inventoryId], references: [id], onDelete: Cascade)

  @@index([inventoryId])
  @@map("inventory_media")
}

model ContentPost {
  id                   String              @id @db.VarChar(26)
  userId               String              @map("user_id") @db.Uuid
  type                 ContentPostType
  title                String
  body                 String
  mainCategoryId       String?             @map("main_category_id") @db.Uuid
  subCategoryId        String?             @map("sub_category_id") @db.Uuid
  categoryId           String?             @map("category_id")
  productGroupId       String?             @map("product_group_id") @db.Uuid
  productId            String?             @map("product_id")
  inventoryRequired    Boolean             @map("inventory_required")
  isBoosted            Boolean             @map("is_boosted")
  boostedUntil         DateTime?           @map("boosted_until")
  createdAt            DateTime            @default(now()) @map("created_at")
  updatedAt            DateTime            @updatedAt @map("updated_at")
  commentsCount        Int                 @default(0) @map("comments_count")
  favoritesCount       Int                 @default(0) @map("favorites_count")
  likesCount           Int                 @default(0) @map("likes_count")
  sharesCount          Int                 @default(0) @map("shares_count")
  viewsCount           Int                 @default(0) @map("views_count")
  experienceDurationId String?             @map("experience_duration_id") @db.Uuid
  experienceLocationId String?             @map("experience_location_id") @db.Uuid
  experiencePurposeId  String?             @map("experience_purpose_id") @db.Uuid
  experienceSnippetId  String?             @map("experience_snippet_id") @db.Uuid
  eventId              String?             @map("event_id") @db.VarChar(26)
  comments             ContentComment[]
  favorites            ContentFavorite[]
  likes                ContentLike[]
  contentPostTags      ContentPostTag[]
  views                ContentPostView[]
  event                WishboxEvent?       @relation(fields: [eventId], references: [id])
  experienceDuration   ExperienceDuration? @relation(fields: [experienceDurationId], references: [id])
  experienceLocation   ExperienceLocation? @relation(fields: [experienceLocationId], references: [id])
  experiencePurpose    ExperiencePurpose?  @relation(fields: [experiencePurposeId], references: [id])
  experienceSnippet    AiExperienceSplit?  @relation(fields: [experienceSnippetId], references: [id])
  mainCategory         MainCategory?       @relation(fields: [mainCategoryId], references: [id])
  productGroup         ProductGroup?       @relation(fields: [productGroupId], references: [id])
  product              Product?            @relation(fields: [productId], references: [id])
  subCategory          SubCategory?        @relation(fields: [subCategoryId], references: [id])
  user                 User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  category             Category?           @relation(fields: [categoryId], references: [id])

  shares              ContentShare[]
  feedHighlights      FeedHighlight[]
  feeds               Feed[]
  comparison          PostComparison?
  media               PostMedia[]
  question            PostQuestion?
  tags                PostTag[]
  tip                 PostTip?
  topCommunityChoices TopCommunityChoice[]
  trendingPosts       TrendingPost[]

  @@index([userId])
  @@index([userId, createdAt])
  @@index([subCategoryId])
  @@index([type])
  @@index([mainCategoryId])
  @@index([productId])
  @@index([createdAt])
  @@index([experienceSnippetId])
  @@index([experienceDurationId])
  @@index([experienceLocationId])
  @@index([experiencePurposeId])
  @@index([eventId])
  @@index([eventId, createdAt])
  @@map("content_posts")
}

model PostMedia {
  id         String      @id @default(uuid()) @db.Uuid
  postId     String      @map("post_id") @db.VarChar(26)
  userId     String      @map("user_id") @db.Uuid
  mediaUrl   String      @map("media_url")
  orderIndex Int         @default(0) @map("order_index")
  createdAt  DateTime    @default(now()) @map("created_at")
  updatedAt  DateTime    @updatedAt @map("updated_at")
  post       ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([postId])
  @@index([postId, orderIndex])
  @@index([userId])
  @@index([userId, createdAt])
  @@map("post_media")
}

model PostQuestion {
  id                   String               @id @default(uuid()) @db.Uuid
  postId               String               @unique @map("post_id") @db.VarChar(26)
  expectedAnswerFormat QuestionAnswerFormat @map("expected_answer_format")
  relatedProductId     String?              @map("related_product_id")
  createdAt            DateTime             @default(now()) @map("created_at")
  updatedAt            DateTime             @updatedAt @map("updated_at")
  post                 ContentPost          @relation(fields: [postId], references: [id], onDelete: Cascade)
  relatedProduct       Product?             @relation(fields: [relatedProductId], references: [id])

  @@map("post_questions")
}

model PostComparison {
  id                String                @id @default(uuid()) @db.Uuid
  postId            String                @unique @map("post_id") @db.VarChar(26)
  product1Id        String                @map("product_1_id")
  product2Id        String                @map("product_2_id")
  comparisonSummary String?               @map("comparison_summary")
  createdAt         DateTime              @default(now()) @map("created_at")
  updatedAt         DateTime              @updatedAt @map("updated_at")
  scores            PostComparisonScore[]
  post              ContentPost           @relation(fields: [postId], references: [id], onDelete: Cascade)
  product1          Product               @relation("ComparisonProduct1", fields: [product1Id], references: [id])
  product2          Product               @relation("ComparisonProduct2", fields: [product2Id], references: [id])

  @@map("post_comparisons")
}

model ComparisonMetric {
  id               String                @id @default(uuid()) @db.Uuid
  name             String
  description      String?
  createdAt        DateTime              @default(now()) @map("created_at")
  updatedAt        DateTime              @updatedAt @map("updated_at")
  comparisonScores PostComparisonScore[]

  @@map("comparison_metrics")
}

model PostComparisonScore {
  id            String           @id @default(uuid()) @db.Uuid
  comparisonId  String           @map("comparison_id") @db.Uuid
  metricId      String           @map("metric_id") @db.Uuid
  scoreProduct1 Int              @map("score_product_1")
  scoreProduct2 Int              @map("score_product_2")
  comment       String?
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")
  comparison    PostComparison   @relation(fields: [comparisonId], references: [id], onDelete: Cascade)
  metric        ComparisonMetric @relation(fields: [metricId], references: [id])

  @@unique([comparisonId, metricId])
  @@map("post_comparison_scores")
}

model PostTip {
  id          String      @id @default(uuid()) @db.Uuid
  postId      String      @unique @map("post_id") @db.VarChar(26)
  tipCategory TipCategory @map("tip_category")
  isVerified  Boolean     @map("is_verified")
  createdAt   DateTime    @default(now()) @map("created_at")
  updatedAt   DateTime    @updatedAt @map("updated_at")
  post        ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@map("post_tips")
}

model PostTag {
  id        String      @id @default(uuid()) @db.Uuid
  postId    String      @map("post_id") @db.VarChar(26)
  tag       String
  createdAt DateTime    @default(now()) @map("created_at")
  updatedAt DateTime    @updatedAt @map("updated_at")
  post      ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([postId])
  @@map("post_tags")
}

model ContentPostTag {
  id        String      @id @default(uuid()) @db.Uuid
  postId    String      @map("post_id") @db.VarChar(26)
  tag       String
  createdAt DateTime    @default(now()) @map("created_at")
  updatedAt DateTime    @updatedAt @map("updated_at")
  post      ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([postId])
  @@map("content_post_tags")
}

model ContentComment {
  id         String               @id @db.VarChar(26)
  postId     String               @map("post_id") @db.VarChar(26)
  userId     String               @map("user_id") @db.Uuid
  parentId   String?              @map("parent_id") @db.VarChar(26)
  comment    String
  isAnswer   Boolean              @map("is_answer")
  createdAt  DateTime             @default(now()) @map("created_at")
  updatedAt  DateTime             @updatedAt @map("updated_at")
  likesCount Int                  @default(0) @map("likes_count")
  votes      ContentCommentVote[]
  parent     ContentComment?      @relation("CommentReplies", fields: [parentId], references: [id])
  replies    ContentComment[]     @relation("CommentReplies")
  post       ContentPost          @relation(fields: [postId], references: [id], onDelete: Cascade)
  user       User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
  likes      ContentLike[]
  ratings    ContentRating[]

  @@index([postId])
  @@index([userId])
  @@index([parentId])
  @@map("content_comments")
}

model ContentLike {
  id        String          @id @default(uuid()) @db.Uuid
  userId    String          @map("user_id") @db.Uuid
  postId    String?         @map("post_id") @db.VarChar(26)
  commentId String?         @map("comment_id") @db.VarChar(26)
  createdAt DateTime        @default(now()) @map("created_at")
  updatedAt DateTime        @updatedAt @map("updated_at")
  comment   ContentComment? @relation(fields: [commentId], references: [id], onDelete: Cascade)
  post      ContentPost?    @relation(fields: [postId], references: [id], onDelete: Cascade)
  user      User            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, postId])
  @@unique([userId, commentId])
  @@index([userId])
  @@index([postId])
  @@index([commentId])
  @@map("content_likes")
}

model ContentFavorite {
  id        String      @id @default(uuid()) @db.Uuid
  userId    String      @map("user_id") @db.Uuid
  postId    String      @map("post_id") @db.VarChar(26)
  createdAt DateTime    @default(now()) @map("created_at")
  updatedAt DateTime    @updatedAt @map("updated_at")
  post      ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  user      User        @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, postId])
  @@index([userId])
  @@index([postId])
  @@map("content_favorites")
}

model ContentShare {
  id        String      @id @default(uuid()) @db.Uuid
  userId    String      @map("user_id") @db.Uuid
  postId    String      @map("post_id") @db.VarChar(26)
  shareType ShareType   @map("share_type")
  platform  String?
  createdAt DateTime    @default(now()) @map("created_at")
  post      ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  user      User        @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, postId])
  @@index([postId])
  @@index([userId])
  @@index([shareType])
  @@map("content_shares")
}

model ContentCollection {
  id          String   @id @default(uuid()) @db.Uuid
  userId      String   @map("user_id") @db.Uuid
  name        String
  description String?
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("content_collections")
}

model ContentRating {
  id        String         @id @default(uuid()) @db.Uuid
  userId    String         @map("user_id") @db.Uuid
  commentId String         @map("comment_id") @db.VarChar(26)
  rating    Int
  createdAt DateTime       @default(now()) @map("created_at")
  updatedAt DateTime       @updatedAt @map("updated_at")
  comment   ContentComment @relation(fields: [commentId], references: [id], onDelete: Cascade)
  user      User           @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, commentId])
  @@index([userId])
  @@index([commentId])
  @@map("content_ratings")
}

model ContentCommentVote {
  id        String         @id @default(uuid()) @db.Uuid
  userId    String         @map("user_id") @db.Uuid
  commentId String         @map("comment_id") @db.VarChar(26)
  voteType  VoteType       @map("vote_type")
  createdAt DateTime       @default(now()) @map("created_at")
  updatedAt DateTime       @updatedAt @map("updated_at")
  comment   ContentComment @relation(fields: [commentId], references: [id], onDelete: Cascade)
  user      User           @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, commentId])
  @@index([userId])
  @@index([commentId])
  @@map("content_comment_votes")
}

model ContentPostView {
  id        String      @id @default(uuid()) @db.Uuid
  postId    String      @map("post_id") @db.VarChar(26)
  userId    String?     @map("user_id") @db.Uuid
  viewedAt  DateTime    @default(now()) @map("viewed_at")
  viewerIp  String      @map("viewer_ip")
  createdAt DateTime    @default(now()) @map("created_at")
  updatedAt DateTime    @updatedAt @map("updated_at")
  post      ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  user      User?       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([postId])
  @@map("content_post_views")
}

model TopCommunityChoice {
  id         String      @id @default(uuid()) @db.Uuid
  postId     String      @map("post_id") @db.VarChar(26)
  reason     String?
  badgeLabel String      @map("badge_label")
  awardedAt  DateTime    @default(now()) @map("awarded_at")
  createdAt  DateTime    @default(now()) @map("created_at")
  updatedAt  DateTime    @updatedAt @map("updated_at")
  post       ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@map("top_community_choices")
}

model Badge {
  id               String            @id @default(uuid()) @db.Uuid
  name             String
  description      String?
  imageUrl         String?           @map("image_url")
  type             BadgeType
  rarity           BadgeRarity
  boostMultiplier  Float?            @map("boost_multiplier")
  rewardMultiplier Float?            @map("reward_multiplier")
  categoryId       String            @map("category_id") @db.Uuid
  createdAt        DateTime          @default(now()) @map("created_at")
  achievementGoals AchievementGoal[]
  category         BadgeCategory     @relation(fields: [categoryId], references: [id])
  bridgeRewards    BridgeReward[]
  cosmeticProfiles Profile[]         @relation("ProfileCosmeticBadge")
  rewardClaims     RewardClaim[]
  userBadges       UserBadge[]

  @@map("badges")
}

model BadgeCategory {
  id          String  @id @default(uuid()) @db.Uuid
  name        String
  description String?
  badges      Badge[]

  @@map("badge_categories")
}

model UserBadge {
  id           String          @id @default(uuid()) @db.Uuid
  userId       String          @map("user_id") @db.Uuid
  badgeId      String          @map("badge_id") @db.Uuid
  isVisible    Boolean         @map("is_visible")
  displayOrder Int?            @map("display_order")
  visibility   BadgeVisibility
  claimed      Boolean         @default(false)
  claimedAt    DateTime?       @map("claimed_at")
  badge        Badge           @relation(fields: [badgeId], references: [id])
  user         User            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, badgeId])
  @@index([userId])
  @@index([badgeId, claimed])
  @@map("user_badges")
}

model AchievementChain {
  id          String            @id @default(uuid()) @db.Uuid
  name        String
  description String?
  category    String
  goals       AchievementGoal[]

  @@map("achievement_chains")
}

model AchievementGoal {
  id               String                @id @default(uuid()) @db.Uuid
  chainId          String                @map("chain_id") @db.Uuid
  title            String
  requirement      String
  rewardBadgeId    String?               @map("reward_badge_id") @db.Uuid
  pointsRequired   Int                   @map("points_required")
  difficulty       AchievementDifficulty
  chain            AchievementChain      @relation(fields: [chainId], references: [id], onDelete: Cascade)
  rewardBadge      Badge?                @relation(fields: [rewardBadgeId], references: [id])
  userAchievements UserAchievement[]

  @@map("achievement_goals")
}

model UserAchievement {
  id          String          @id @default(uuid()) @db.Uuid
  userId      String          @map("user_id") @db.Uuid
  goalId      String          @map("goal_id") @db.Uuid
  progress    Int
  completed   Boolean         @default(false)
  completedAt DateTime?       @map("completed_at")
  goal        AchievementGoal @relation(fields: [goalId], references: [id], onDelete: Cascade)
  user        User            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, goalId])
  @@map("user_achievements")
}

model RewardClaim {
  id            String        @id @default(uuid()) @db.Uuid
  userId        String        @map("user_id") @db.Uuid
  badgeId       String        @map("badge_id") @db.Uuid
  amount        Float
  claimedAt     DateTime      @default(now()) @map("claimed_at")
  vestingStatus VestingStatus @map("vesting_status")
  badge         Badge         @relation(fields: [badgeId], references: [id])
  user          User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("reward_claims")
}

model BrandCategory {
  id        String   @id @default(uuid()) @db.Uuid
  name      String   @unique
  imageUrl  String?  @map("image_url")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  brands    Brand[]

  @@map("brand_categories")
}

model Brand {
  id            String              @id @default(uuid()) @db.Uuid
  externalId    String?             @unique @map("external_id")
  name          String
  description   String?
  logoUrl       String?             @map("logo_url")
  category      String?
  createdAt     DateTime            @default(now()) @map("created_at")
  updatedAt     DateTime            @updatedAt @map("updated_at")
  imageUrl      String?             @map("image_url")
  categoryId    String?             @map("category_id") @db.Uuid
  surveys       BrandSurvey[]
  brandCategory BrandCategory?      @relation(fields: [categoryId], references: [id])
  followers     BridgeFollower[]
  leaderboards  BridgeLeaderboard[]
  bridgePosts   BridgePost[]
  rewards       BridgeReward[]
  userStats     BridgeUserStats[]
  wishboxEvents WishboxEvent[]
  products      Product[]

  @@map("brands")
}

model BridgePost {
  id        String   @id @db.VarChar(26)
  brandId   String   @map("brand_id") @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  content   String
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  brand     Brand    @relation(fields: [brandId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([brandId])
  @@index([userId])
  @@map("bridge_posts")
}

model BrandSurvey {
  id          String                @id @default(uuid()) @db.Uuid
  brandId     String                @map("brand_id") @db.Uuid
  title       String
  description String?
  startsAt    DateTime              @map("starts_at")
  endsAt      DateTime              @map("ends_at")
  createdAt   DateTime              @default(now()) @map("created_at")
  updatedAt   DateTime              @updatedAt @map("updated_at")
  questions   BrandSurveyQuestion[]
  brand       Brand                 @relation(fields: [brandId], references: [id], onDelete: Cascade)

  @@index([brandId])
  @@map("brand_surveys")
}

model BrandSurveyQuestion {
  id           String              @id @default(uuid()) @db.Uuid
  surveyId     String              @map("survey_id") @db.Uuid
  questionText String              @map("question_text")
  type         SurveyQuestionType
  createdAt    DateTime            @default(now()) @map("created_at")
  updatedAt    DateTime            @updatedAt @map("updated_at")
  answers      BrandSurveyAnswer[]
  survey       BrandSurvey         @relation(fields: [surveyId], references: [id], onDelete: Cascade)

  @@map("brand_survey_questions")
}

model BrandSurveyAnswer {
  id         String              @id @default(uuid()) @db.Uuid
  questionId String              @map("question_id") @db.Uuid
  userId     String              @map("user_id") @db.Uuid
  answerText String              @map("answer_text")
  createdAt  DateTime            @default(now()) @map("created_at")
  updatedAt  DateTime            @updatedAt @map("updated_at")
  question   BrandSurveyQuestion @relation(fields: [questionId], references: [id], onDelete: Cascade)
  user       User                @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([questionId, userId])
  @@index([questionId])
  @@index([userId])
  @@map("brand_survey_answers")
}

model BridgeFollower {
  id         String   @id @default(uuid()) @db.Uuid
  userId     String   @map("user_id") @db.Uuid
  brandId    String   @map("brand_id") @db.Uuid
  followedAt DateTime @default(now()) @map("followed_at")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")
  brand      Brand    @relation(fields: [brandId], references: [id], onDelete: Cascade)
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, brandId])
  @@map("bridge_followers")
}

model BridgeUserStats {
  id                  String    @id @default(uuid()) @db.Uuid
  userId              String    @map("user_id") @db.Uuid
  brandId             String    @map("brand_id") @db.Uuid
  commentsCount       Int       @map("comments_count")
  surveysParticipated Int       @map("surveys_participated")
  trustScore          Float     @map("trust_score")
  lastInteractionAt   DateTime? @map("last_interaction_at")
  createdAt           DateTime  @default(now()) @map("created_at")
  updatedAt           DateTime  @updatedAt @map("updated_at")
  brand               Brand     @relation(fields: [brandId], references: [id], onDelete: Cascade)
  user                User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, brandId])
  @@map("bridge_user_stats")
}

model BridgeLeaderboard {
  id         String            @id @default(uuid()) @db.Uuid
  brandId    String            @map("brand_id") @db.Uuid
  userId     String            @map("user_id") @db.Uuid
  period     LeaderboardPeriod
  rank       Int
  score      Float
  recordedAt DateTime          @default(now()) @map("recorded_at")
  createdAt  DateTime          @default(now()) @map("created_at")
  updatedAt  DateTime          @updatedAt @map("updated_at")
  brand      Brand             @relation(fields: [brandId], references: [id], onDelete: Cascade)
  user       User              @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([brandId, userId, period])
  @@map("bridge_leaderboards")
}

model BridgeReward {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @map("user_id") @db.Uuid
  brandId   String   @map("brand_id") @db.Uuid
  badgeId   String   @map("badge_id") @db.Uuid
  awardedAt DateTime @default(now()) @map("awarded_at")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  badge     Badge    @relation(fields: [badgeId], references: [id], onDelete: Cascade)
  brand     Brand    @relation(fields: [brandId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("bridge_rewards")
}

model Wallet {
  id            String         @id @default(uuid()) @db.Uuid
  userId        String         @map("user_id") @db.Uuid
  publicAddress String         @map("public_address")
  provider      WalletProvider
  isConnected   Boolean        @map("is_connected")
  createdAt     DateTime       @default(now()) @map("created_at")
  updatedAt     DateTime       @updatedAt @map("updated_at")
  user          User           @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, provider])
  @@map("wallets")
}

model TipsTokenTransfer {
  id         String   @id @default(uuid()) @db.Uuid
  fromUserId String   @map("from_user_id") @db.Uuid
  toUserId   String   @map("to_user_id") @db.Uuid
  amount     Float
  reason     String?
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")
  fromUser   User     @relation("SentTips", fields: [fromUserId], references: [id], onDelete: Cascade)
  toUser     User     @relation("ReceivedTips", fields: [toUserId], references: [id], onDelete: Cascade)

  @@index([fromUserId])
  @@index([toUserId])
  @@map("tips_token_transfers")
}

model NFT {
  id             String             @id @default(uuid()) @db.Uuid
  name           String
  description    String?
  imageUrl       String             @map("image_url")
  type           NFTType
  rarity         NFTRarity
  isTransferable Boolean            @map("is_transferable")
  currentOwnerId String?            @map("current_owner_id") @db.Uuid
  createdAt      DateTime           @default(now()) @map("created_at")
  updatedAt      DateTime           @updatedAt @map("updated_at")
  attributes     NFTAttribute[]
  claims         NFTClaim[]
  marketListings NFTMarketListing[]
  transactions   NFTTransaction[]
  currentOwner   User?              @relation("NFTOwner", fields: [currentOwnerId], references: [id])

  @@index([currentOwnerId])
  @@index([name])
  @@index([type, rarity])
  @@map("nfts")
}

model NFTTransaction {
  id              String             @id @default(uuid()) @db.Uuid
  nftId           String             @map("nft_id") @db.Uuid
  fromUserId      String?            @map("from_user_id") @db.Uuid
  toUserId        String             @map("to_user_id") @db.Uuid
  price           Float?
  transactionType NFTTransactionType @map("transaction_type")
  createdAt       DateTime           @default(now()) @map("created_at")
  updatedAt       DateTime           @updatedAt @map("updated_at")
  fromUser        User?              @relation("NFTSender", fields: [fromUserId], references: [id])
  nft             NFT                @relation(fields: [nftId], references: [id], onDelete: Cascade)
  toUser          User               @relation("NFTReceiver", fields: [toUserId], references: [id])

  @@index([nftId])
  @@index([toUserId])
  @@index([fromUserId])
  @@map("nft_transactions")
}

model NFTMarketListing {
  id             String                 @id @default(uuid()) @db.Uuid
  nftId          String                 @map("nft_id") @db.Uuid
  listedByUserId String                 @map("listed_by_user_id") @db.Uuid
  price          Float
  status         NFTMarketListingStatus @default(ACTIVE)
  listedAt       DateTime               @default(now()) @map("listed_at")
  createdAt      DateTime               @default(now()) @map("created_at")
  updatedAt      DateTime               @updatedAt @map("updated_at")
  listedByUser   User                   @relation(fields: [listedByUserId], references: [id], onDelete: Cascade)
  nft            NFT                    @relation(fields: [nftId], references: [id], onDelete: Cascade)

  @@index([status])
  @@index([nftId])
  @@index([listedByUserId])
  @@index([status, price])
  @@map("nft_market_listings")
}

model NFTAttribute {
  id        String   @id @default(uuid()) @db.Uuid
  nftId     String   @map("nft_id") @db.Uuid
  key       String
  value     String
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  nft       NFT      @relation(fields: [nftId], references: [id], onDelete: Cascade)

  @@map("nft_attributes")
}

model Lootbox {
  id         String        @id @default(uuid()) @db.Uuid
  userId     String        @map("user_id") @db.Uuid
  tipsLocked Float         @map("tips_locked")
  status     LootboxStatus @default(LOCKED)
  createdAt  DateTime      @default(now()) @map("created_at")
  updatedAt  DateTime      @updatedAt @map("updated_at")
  user       User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([status])
  @@map("lootboxes")
}

model NFTClaim {
  id         String   @id @default(uuid()) @db.Uuid
  userId     String   @map("user_id") @db.Uuid
  nftId      String?  @map("nft_id") @db.Uuid
  tipsAmount Float    @map("tips_amount")
  claimedAt  DateTime @default(now()) @map("claimed_at")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")
  nft        NFT?     @relation(fields: [nftId], references: [id])
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("nft_claims")
}

model WishboxEvent {
  id          String             @id @db.VarChar(26)
  title       String
  description String?
  startDate   DateTime           @map("start_date")
  endDate     DateTime           @map("end_date")
  status      WishboxEventStatus @default(DRAFT)
  createdAt   DateTime           @default(now()) @map("created_at")
  updatedAt   DateTime           @updatedAt @map("updated_at")
  eventType   WishboxEventType   @default(SURVEY) @map("event_type")
  imageUrl    String?            @map("image_url")
  brandId     String?            @map("brand_id") @db.Uuid
  posts       ContentPost[]
  brand       Brand?             @relation(fields: [brandId], references: [id], onDelete: Cascade)
  rewards     WishboxReward[]
  scenarios   WishboxScenario[]
  stats       WishboxStats[]

  @@index([brandId])
  @@map("wishbox_events")
}

model WishboxScenario {
  id          String           @id @default(uuid()) @db.Uuid
  eventId     String           @map("event_id") @db.VarChar(26)
  title       String
  description String?
  orderIndex  Int              @map("order_index")
  createdAt   DateTime         @default(now()) @map("created_at")
  updatedAt   DateTime         @updatedAt @map("updated_at")
  choices     ScenarioChoice[]
  event       WishboxEvent     @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@map("wishbox_scenarios")
}

model ScenarioChoice {
  id         String          @id @default(uuid()) @db.Uuid
  scenarioId String          @map("scenario_id") @db.Uuid
  userId     String          @map("user_id") @db.Uuid
  choiceText String          @map("choice_text")
  isSelected Boolean         @map("is_selected")
  createdAt  DateTime        @default(now()) @map("created_at")
  updatedAt  DateTime        @updatedAt @map("updated_at")
  comments   ChoiceComment[]
  scenario   WishboxScenario @relation(fields: [scenarioId], references: [id], onDelete: Cascade)
  user       User            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([scenarioId, userId])
  @@map("scenario_choices")
}

model ChoiceComment {
  id        String         @id @default(uuid()) @db.Uuid
  choiceId  String         @map("choice_id") @db.Uuid
  userId    String         @map("user_id") @db.Uuid
  comment   String
  createdAt DateTime       @default(now()) @map("created_at")
  updatedAt DateTime       @updatedAt @map("updated_at")
  choice    ScenarioChoice @relation(fields: [choiceId], references: [id], onDelete: Cascade)
  user      User           @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([choiceId])
  @@index([userId])
  @@map("choice_comments")
}

model WishboxStats {
  id                   String       @id @default(uuid()) @db.Uuid
  userId               String       @map("user_id") @db.Uuid
  eventId              String       @map("event_id") @db.VarChar(26)
  totalParticipated    Int          @map("total_participated")
  totalComments        Int          @map("total_comments")
  helpfulVotesReceived Int          @map("helpful_votes_received")
  createdAt            DateTime     @default(now()) @map("created_at")
  updatedAt            DateTime     @updatedAt @map("updated_at")
  event                WishboxEvent @relation(fields: [eventId], references: [id], onDelete: Cascade)
  user                 User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, eventId])
  @@map("wishbox_stats")
}

model WishboxReward {
  id         String            @id @default(uuid()) @db.Uuid
  userId     String            @map("user_id") @db.Uuid
  eventId    String            @map("event_id") @db.VarChar(26)
  rewardType WishboxRewardType @map("reward_type")
  rewardId   Int               @map("reward_id")
  amount     Float?
  awardedAt  DateTime          @default(now()) @map("awarded_at")
  createdAt  DateTime          @default(now()) @map("created_at")
  updatedAt  DateTime          @updatedAt @map("updated_at")
  event      WishboxEvent      @relation(fields: [eventId], references: [id], onDelete: Cascade)
  user       User              @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([eventId])
  @@map("wishbox_rewards")
}

model DMThread {
  id                 String             @id @default(uuid()) @db.Uuid
  userOneId          String             @map("user_one_id") @db.Uuid
  userTwoId          String             @map("user_two_id") @db.Uuid
  isActive           Boolean            @map("is_active")
  startedAt          DateTime           @default(now()) @map("started_at")
  createdAt          DateTime           @default(now()) @map("created_at")
  updatedAt          DateTime           @updatedAt @map("updated_at")
  unreadCountUserOne Int                @default(0) @map("unread_count_user_one")
  unreadCountUserTwo Int                @default(0) @map("unread_count_user_two")
  isSupportThread    Boolean            @default(false) @map("is_support_thread")
  messages           DMMessage[]
  supportRequests    DMRequest[]        @relation("DMRequestThread")
  supportSessions    DMSupportSession[]
  userOne            User               @relation("DMThreadUserOne", fields: [userOneId], references: [id], onDelete: Cascade)
  userTwo            User               @relation("DMThreadUserTwo", fields: [userTwoId], references: [id], onDelete: Cascade)

  @@index([userOneId])
  @@index([userTwoId])
  @@index([isSupportThread])
  @@map("dm_threads")
}

model DMMessage {
  id        String           @id @default(uuid()) @db.Uuid
  threadId  String           @map("thread_id") @db.Uuid
  senderId  String           @map("sender_id") @db.Uuid
  message   String
  sentAt    DateTime         @default(now()) @map("sent_at")
  isRead    Boolean          @map("is_read")
  createdAt DateTime         @default(now()) @map("created_at")
  updatedAt DateTime         @updatedAt @map("updated_at")
  context   DMMessageContext @default(DM)
  sender    User             @relation(fields: [senderId], references: [id], onDelete: Cascade)
  thread    DMThread         @relation(fields: [threadId], references: [id], onDelete: Cascade)

  @@index([threadId])
  @@index([threadId, isRead])
  @@index([senderId])
  @@map("dm_messages")
}

model DMRequest {
  id                 String                 @id @default(uuid()) @db.Uuid
  fromUserId         String                 @map("from_user_id") @db.Uuid
  toUserId           String                 @map("to_user_id") @db.Uuid
  status             DMRequestStatus        @default(PENDING)
  sentAt             DateTime               @default(now()) @map("sent_at")
  respondedAt        DateTime?              @map("responded_at")
  createdAt          DateTime               @default(now()) @map("created_at")
  updatedAt          DateTime               @updatedAt @map("updated_at")
  amount             Float                  @default(0)
  description        String?
  type               SupportType            @default(GENERAL)
  threadId           String?                @map("thread_id") @db.Uuid
  fromUserRating     Int?                   @map("from_user_rating")
  toUserRating       Int?                   @map("to_user_rating")
  closedByFromUserAt DateTime?              @map("closed_by_from_user_at")
  closedByToUserAt   DateTime?              @map("closed_by_to_user_at")
  fromUser           User                   @relation("DMRequestSender", fields: [fromUserId], references: [id], onDelete: Cascade)
  thread             DMThread?              @relation("DMRequestThread", fields: [threadId], references: [id])
  toUser             User                   @relation("DMRequestReceiver", fields: [toUserId], references: [id], onDelete: Cascade)
  reports            SupportRequestReport[]

  @@index([fromUserId, toUserId])
  @@index([fromUserId])
  @@index([toUserId])
  @@index([status])
  @@index([threadId])
  @@map("dm_requests")
}

model SupportRequestReport {
  id          String    @id @default(uuid()) @db.Uuid
  requestId   String    @map("request_id") @db.Uuid
  reporterId  String    @map("reporter_id") @db.Uuid
  category    String
  description String?
  createdAt   DateTime  @default(now()) @map("created_at")
  reporter    User      @relation("SupportRequestReportReporter", fields: [reporterId], references: [id], onDelete: Cascade)
  request     DMRequest @relation(fields: [requestId], references: [id], onDelete: Cascade)

  @@index([requestId])
  @@index([reporterId])
  @@map("support_request_reports")
}

model UserReport {
  id             String   @id @default(uuid()) @db.Uuid
  reportedUserId String   @map("reported_user_id") @db.Uuid
  reporterId     String   @map("reporter_id") @db.Uuid
  category       String
  description    String?
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")
  reportedUser   User     @relation("UserReportReported", fields: [reportedUserId], references: [id], onDelete: Cascade)
  reporter       User     @relation("UserReportReporter", fields: [reporterId], references: [id], onDelete: Cascade)

  @@unique([reporterId, reportedUserId])
  @@index([reportedUserId])
  @@index([reporterId])
  @@index([category])
  @@map("user_reports")
}

model DMSupportSession {
  id          String       @id @default(uuid()) @db.Uuid
  threadId    String       @map("thread_id") @db.Uuid
  helperId    String       @map("helper_id") @db.Uuid
  tipsAmount  Float        @map("tips_amount")
  supportedAt DateTime     @default(now()) @map("supported_at")
  createdAt   DateTime     @default(now()) @map("created_at")
  updatedAt   DateTime     @updatedAt @map("updated_at")
  feedbacks   DMFeedback[]
  helper      User         @relation(fields: [helperId], references: [id], onDelete: Cascade)
  thread      DMThread     @relation(fields: [threadId], references: [id], onDelete: Cascade)

  @@index([threadId])
  @@index([helperId])
  @@map("dm_support_sessions")
}

model DMFeedback {
  id          String           @id @default(uuid()) @db.Uuid
  sessionId   String           @map("session_id") @db.Uuid
  rating      Int
  comment     String?
  submittedAt DateTime         @default(now()) @map("submitted_at")
  createdAt   DateTime         @default(now()) @map("created_at")
  updatedAt   DateTime         @updatedAt @map("updated_at")
  session     DMSupportSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@map("dm_feedbacks")
}

model Feed {
  id             String      @id @db.VarChar(26)
  userId         String      @map("user_id") @db.Uuid
  postId         String      @map("post_id") @db.VarChar(26)
  source         FeedSource
  seen           Boolean     @default(false)
  createdAt      DateTime    @default(now()) @map("created_at")
  updatedAt      DateTime    @updatedAt @map("updated_at")
  relevanceScore Float       @default(0) @map("relevance_score")
  post           ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  user           User        @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([userId, seen])
  @@index([userId, source])
  @@index([postId])
  @@index([userId, relevanceScore(sort: Desc)])
  @@index([relevanceScore(sort: Desc), createdAt(sort: Desc)])
  @@index([userId, relevanceScore(sort: Desc), createdAt(sort: Desc)])
  @@map("feeds")
}

model FeedHighlight {
  id            String              @id @db.VarChar(26)
  postId        String              @map("post_id") @db.VarChar(26)
  reason        FeedHighlightReason
  highlightedAt DateTime            @default(now()) @map("highlighted_at")
  createdAt     DateTime            @default(now()) @map("created_at")
  updatedAt     DateTime            @updatedAt @map("updated_at")
  post          ContentPost         @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([postId])
  @@map("feed_highlights")
}

model TrendingPost {
  id           String      @id @db.VarChar(26)
  postId       String      @map("post_id") @db.VarChar(26)
  score        Float
  trendPeriod  TrendPeriod @map("trend_period")
  calculatedAt DateTime    @default(now()) @map("calculated_at")
  createdAt    DateTime    @default(now()) @map("created_at")
  updatedAt    DateTime    @updatedAt @map("updated_at")
  post         ContentPost @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@unique([postId, trendPeriod])
  @@map("trending_posts")
}

model AdminLog {
  id          String   @id @default(uuid()) @db.Uuid
  adminId     String   @map("admin_id") @db.Uuid
  action      String
  description String?
  entityType  String   @map("entity_type")
  entityId    Int      @map("entity_id")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  admin       User     @relation(fields: [adminId], references: [id], onDelete: Cascade)

  @@index([adminId])
  @@map("admin_logs")
}

model ModerationAction {
  id           String               @id @default(uuid()) @db.Uuid
  moderatorId  String               @map("moderator_id") @db.Uuid
  targetUserId String               @map("target_user_id") @db.Uuid
  reason       String
  actionType   ModerationActionType @map("action_type")
  contentType  String?              @map("content_type")
  contentId    Int?                 @map("content_id")
  createdAt    DateTime             @default(now()) @map("created_at")
  updatedAt    DateTime             @updatedAt @map("updated_at")
  moderator    User                 @relation("ModeratorActions", fields: [moderatorId], references: [id], onDelete: Cascade)
  targetUser   User                 @relation("ModeratedActions", fields: [targetUserId], references: [id], onDelete: Cascade)

  @@index([moderatorId])
  @@index([targetUserId])
  @@map("moderation_actions")
}

model ManualReviewFlag {
  id              String                 @id @default(uuid()) @db.Uuid
  flaggedByUserId String                 @map("flagged_by_user_id") @db.Uuid
  contentType     String                 @map("content_type")
  contentId       Int                    @map("content_id")
  reason          String
  status          ManualReviewFlagStatus @default(OPEN)
  createdAt       DateTime               @default(now()) @map("created_at")
  updatedAt       DateTime               @updatedAt @map("updated_at")
  flaggedByUser   User                   @relation(fields: [flaggedByUserId], references: [id], onDelete: Cascade)

  @@map("manual_review_flags")
}

model MarketplaceBanner {
  id           String    @id @default(uuid()) @db.Uuid
  title        String
  description  String?
  imageUrl     String    @map("image_url")
  linkUrl      String?   @map("link_url")
  isActive     Boolean   @default(true) @map("is_active")
  displayOrder Int       @map("display_order")
  startDate    DateTime? @map("start_date")
  endDate      DateTime? @map("end_date")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @updatedAt @map("updated_at")

  @@map("marketplace_banners")
}

model ExpertRequest {
  id          String               @id @default(uuid()) @db.Uuid
  userId      String               @map("user_id") @db.Uuid
  description String
  category    String?              @map("category")
  tipsAmount  Float                @default(0) @map("tips_amount")
  status      ExpertRequestStatus  @default(PENDING)
  answeredAt  DateTime?            @map("answered_at")
  createdAt   DateTime             @default(now()) @map("created_at")
  updatedAt   DateTime             @updatedAt @map("updated_at")
  answers     ExpertAnswer[]
  media       ExpertRequestMedia[]
  user        User                 @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([status])
  @@map("expert_requests")
}

model ExpertRequestMedia {
  id            String        @id @default(uuid()) @db.Uuid
  requestId     String        @map("request_id") @db.Uuid
  mediaUrl      String        @map("media_url")
  mediaType     String        @map("media_type")
  uploadedAt    DateTime      @default(now()) @map("uploaded_at")
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")
  expertRequest ExpertRequest @relation(fields: [requestId], references: [id], onDelete: Cascade)

  @@index([requestId])
  @@map("expert_request_media")
}

model ExpertAnswer {
  id           String        @id @default(uuid()) @db.Uuid
  requestId    String        @map("request_id") @db.Uuid
  expertUserId String        @map("expert_user_id") @db.Uuid
  content      String
  createdAt    DateTime      @default(now()) @map("created_at")
  updatedAt    DateTime      @updatedAt @map("updated_at")
  expertUser   User          @relation(fields: [expertUserId], references: [id], onDelete: Cascade)
  request      ExpertRequest @relation(fields: [requestId], references: [id], onDelete: Cascade)

  @@unique([requestId, expertUserId])
  @@index([requestId])
  @@index([expertUserId])
  @@map("expert_answers")
}

model AiExperienceSplit {
  id                          String        @id @default(uuid()) @db.Uuid
  userId                      String        @map("user_id") @db.Uuid
  productId                   String?       @map("product_id")
  originalExperience          String        @map("original_experience")
  priceAndShopping            String?       @map("price_and_shopping")
  productAndUsage             String?       @map("product_and_usage")
  priceAndShoppingRating      Int?          @map("price_and_shopping_rating")
  productAndUsageRating       Int?          @map("product_and_usage_rating")
  isEdited                    Boolean       @default(false) @map("is_edited")
  model                       String        @default("gemini-2.5-pro")
  promptVersion               String        @default("v1.0") @map("prompt_version")
  tokensUsed                  Int?          @map("tokens_used")
  processingTimeMs            Int?          @map("processing_time_ms")
  createdAt                   DateTime      @default(now()) @map("created_at")
  updatedAt                   DateTime      @updatedAt @map("updated_at")
  priceAndShoppingIsEnhanced  Boolean?      @map("price_and_shopping_is_enhanced")
  priceAndShoppingPlaceholder String?       @map("price_and_shopping_placeholder")
  productAndUsageIsEnhanced   Boolean?      @map("product_and_usage_is_enhanced")
  productAndUsagePlaceholder  String?       @map("product_and_usage_placeholder")
  product                     Product?      @relation("ProductAiExperienceSplits", fields: [productId], references: [id])
  user                        User          @relation("UserAiExperienceSplits", fields: [userId], references: [id], onDelete: Cascade)
  contentPosts                ContentPost[]
  inventories                 Inventory[]

  @@index([userId])
  @@index([productId])
  @@index([createdAt])
  @@map("ai_experience_splits")
}

model BoostOption {
  id          String   @id @default(uuid()) @db.Uuid
  image       String?
  title       String
  description String?
  amount      Float
  isPopular   Boolean  @default(false) @map("is_popular")
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  @@map("boost_options")
}

model ExperienceDuration {
  id           String        @id @default(uuid()) @db.Uuid
  name         String        @unique
  isActive     Boolean       @default(true) @map("is_active")
  createdAt    DateTime      @default(now()) @map("created_at")
  updatedAt    DateTime      @updatedAt @map("updated_at")
  contentPosts ContentPost[]
  inventories  Inventory[]

  @@map("experience_durations")
}

model ExperienceLocation {
  id           String        @id @default(uuid()) @db.Uuid
  name         String        @unique
  isActive     Boolean       @default(true) @map("is_active")
  createdAt    DateTime      @default(now()) @map("created_at")
  updatedAt    DateTime      @updatedAt @map("updated_at")
  contentPosts ContentPost[]
  inventories  Inventory[]

  @@map("experience_locations")
}

model ExperiencePurpose {
  id           String        @id @default(uuid()) @db.Uuid
  name         String        @unique
  isActive     Boolean       @default(true) @map("is_active")
  createdAt    DateTime      @default(now()) @map("created_at")
  updatedAt    DateTime      @updatedAt @map("updated_at")
  contentPosts ContentPost[]
  inventories  Inventory[]

  @@map("experience_purposes")
}

enum DMMessageContext {
  DM
  SUPPORT
}

enum KycReviewStatus {
  INIT
  PENDING
  COMPLETED
  DECLINED
  ON_HOLD

  @@map("kyc_review_status")
}

enum KycReviewResult {
  NULL
  GREEN
  YELLOW
  RED

  @@map("kyc_review_result")
}

enum KycProvider {
  SUMSUB

  @@map("kyc_provider")
}

enum UserVisibility {
  PUBLIC
  PRIVATE
  FRIENDS_ONLY

  @@map("user_visibility")
}

enum LoginAttemptStatus {
  SUCCESS
  FAILED
  LOCKED

  @@map("login_attempt_status")
}

enum ProductSuggestionStatus {
  PENDING
  APPROVED
  REJECTED

  @@map("product_suggestion_status")
}

enum ContentPostType {
  FREE
  TIPS
  COMPARE
  QUESTION
  EXPERIENCE
  UPDATE

  @@map("content_post_type")
}

enum ShareType {
  INTERNAL_REPOST
  EXTERNAL_SHARE

  @@map("share_type")
}

enum QuestionAnswerFormat {
  SHORT
  LONG
  POLL
  CHOICE

  @@map("question_answer_format")
}

enum TipCategory {
  USAGE
  PURCHASE
  CARE
  OTHER

  @@map("tip_category")
}

enum VoteType {
  UPVOTE
  DOWNVOTE

  @@map("vote_type")
}

enum BadgeType {
  ACHIEVEMENT
  EVENT
  COSMETIC

  @@map("badge_type")
}

enum BadgeRarity {
  COMMON
  RARE
  EPIC

  @@map("badge_rarity")
}

enum BadgeVisibility {
  PUBLIC
  FRIENDS
  TRUSTERS
  PRIVATE

  @@map("badge_visibility")
}

enum AchievementDifficulty {
  EASY
  MEDIUM
  HARD

  @@map("achievement_difficulty")
}

enum VestingStatus {
  PENDING
  PARTIAL
  COMPLETED

  @@map("vesting_status")
}

enum SurveyQuestionType {
  SINGLE_CHOICE
  MULTIPLE_CHOICE
  TEXT

  @@map("survey_question_type")
}

enum LeaderboardPeriod {
  WEEKLY
  MONTHLY

  @@map("leaderboard_period")
}

enum WalletProvider {
  METAMASK
  WALLETCONNECT
  CUSTOM

  @@map("wallet_provider")
}

enum NFTType {
  BADGE
  COSMETIC
  LOOTBOX

  @@map("nft_type")
}

enum NFTRarity {
  COMMON
  RARE
  EPIC

  @@map("nft_rarity")
}

enum NFTTransactionType {
  MINT
  TRANSFER
  PURCHASE

  @@map("nft_transaction_type")
}

enum NFTMarketListingStatus {
  ACTIVE
  SOLD
  CANCELLED

  @@map("nft_market_listing_status")
}

enum LootboxStatus {
  LOCKED
  OPENABLE
  CLAIMED

  @@map("lootbox_status")
}

enum WishboxEventStatus {
  DRAFT
  PUBLISHED
  CLOSED

  @@map("wishbox_event_status")
}

enum WishboxEventType {
  SURVEY
  POLL
  CONTEST
  CHALLENGE
  PROMOTION

  @@map("wishbox_event_type")
}

enum WishboxRewardType {
  TIPS
  BADGE
  TITLE

  @@map("wishbox_reward_type")
}

enum DMRequestStatus {
  PENDING
  ACCEPTED
  DECLINED
  CANCELED
  AWAITING_COMPLETION
  COMPLETED
  REPORTED

  @@map("dm_request_status")
}

enum SupportType {
  GENERAL
  TECHNICAL
  PRODUCT

  @@map("support_type")
}

enum FeedSource {
  TRUSTER
  CATEGORY_MATCH
  TRENDING
  NEW_USER
  BOOSTED
  TRUSTER_NETWORK
  MUTUAL_TRUST
  INVENTORY_MATCH
  PRODUCT_GROUP_MATCH
  ENGAGEMENT_HIGH

  @@map("feed_source")
}

enum FeedHighlightReason {
  MOST_LIKED
  STAFF_PICK
  BOOSTED

  @@map("feed_highlight_reason")
}

enum TrendPeriod {
  DAILY
  WEEKLY

  @@map("trend_period")
}

enum ModerationActionType {
  BAN
  WARN
  MUTE
  CONTENT_REMOVED

  @@map("moderation_action_type")
}

enum ManualReviewFlagStatus {
  OPEN
  IN_REVIEW
  RESOLVED

  @@map("manual_review_flag_status")
}

enum ExpertRequestStatus {
  PENDING
  BROADCASTING
  EXPERT_FOUND
  ANSWERED
  CLOSED

  @@map("expert_request_status")
}

model Notification {
  id        String           @id @default(uuid()) @db.Uuid
  userId    String           @map("user_id") @db.Uuid
  type      NotificationType
  title     String
  message   String
  data      Json?
  read      Boolean          @default(false)
  readAt    DateTime?        @map("read_at")
  createdAt DateTime         @default(now()) @map("created_at")
  updatedAt DateTime         @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, read])
  @@index([userId, createdAt])
  @@index([type])
  @@map("notifications")
}

model PushToken {
  id         String   @id @default(uuid()) @db.Uuid
  userId     String   @map("user_id") @db.Uuid
  token      String   @unique
  deviceType String   @map("device_type")
  isActive   Boolean  @default(true) @map("is_active")
  lastUsedAt DateTime @default(now()) @map("last_used_at")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([token, isActive])
  @@map("push_tokens")
}

enum NotificationType {
  // Interaction Notifications
  POST_LIKED
  POST_COMMENTED
  POST_SHARED
  POST_FAVORITED
  COMMENT_LIKED
  COMMENT_REPLIED

  // Trust & Follow Notifications
  NEW_TRUSTER
  NEW_TRUSTED_BY

  // Messaging Notifications
  NEW_MESSAGE
  DM_REQUEST_RECEIVED
  DM_REQUEST_ACCEPTED
  SUPPORT_REQUEST_ACCEPTED

  // Collection Notifications
  COLLECTION_POST_ADDED
  COLLECTION_SHARED

  // Gamification Notifications
  NEW_BADGE
  ACHIEVEMENT_UNLOCKED
  LEVEL_UP
  REWARD_EARNED

  // Expert Notifications
  EXPERT_REQUEST_AVAILABLE
  EXPERT_REQUEST_ANSWERED

  // Event Notifications
  EVENT_STARTED
  EVENT_ENDING_SOON
  EVENT_REWARD_AVAILABLE

  // System Notifications
  SYSTEM_ANNOUNCEMENT
  ACCOUNT_SECURITY
  TIPS_RECEIVED
  TIPS_SENT

  @@map("notification_type")
}
