# .env örneği — SADECE .env'de kalması gereken değişkenler (gizli / docker-compose'ta yok).
# PORT, DATABASE_URL, REDIS_URL, CORS_*, S3_*, BASE_URL, GEMINI_MODEL, THIRDWEB_WALLET_ID vb.
# docker-compose.yml backend environment içinde tanımlı; burada tekrarlanmaz.
#
# CORS: Thirdweb webhook isteklerinin host'u (portal.thirdweb.com, thirdweb.com, engine.thirdweb.com)
# backend tarafından otomatik olarak CORS'a eklenir; CORS_ORIGINS set etseniz bile bu origin'ler listeye merge edilir.

# Auth0
AUTH0_DOMAIN=
AUTH0_AUDIENCE=
ISSUER_BASE_URL=
CLIENT_ID=
CLIENT_SECRET=
SECRET=
# Opsiyonel: Issuer discovery timeout (ms). Varsayılan 15000. Yavaş ağ/VPN için artırılabilir.
# AUTH0_HTTP_TIMEOUT_MS=20000

# JWT
JWT_SECRET=

# Canny SSO (Admin > Settings > Security > SSO redirect başlığında Private Key)
# https://help.canny.io/en/articles/1961021-setting-up-a-single-sign-on-sso-redirect
#https://developers.canny.io/install/widget/sso
CANNY_SSO_PRIVATE_KEY=

# Thirdweb (gizli / API key)
THIRDWEB_CLIENT_ID=
THIRDWEB_SECRET_KEY=
ADMIN_PRIVATE_KEY=
THIRDWEB_WEBHOOK_SECRET=
THIRDWEB_EVENTS_WEBHOOK_SECRET=

# Alchemy Notify Webhook (Dashboard > Webhooks > AUTH TOKEN / signing key). Test için: whsec_test. (ngrok ile test önerilir)
ALCHEMY_WEBHOOK_SIGNING_KEY=

# Google OAuth / Gmail
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
EMAIL_USER_TO_IMPERSONATE=

# Gemini / Translate API (gizli)
GEMINI_API_KEY=
GOOGLE_TRANSLATE_API_KEY=

# Thirdweb (opsiyonel)
THIRDWEB_ACCOUNT_FACTORY_ADDRESS=
THIRDWEB_ECOSYSTEM_ID=

# MinIO CORS (badge/media görsellerine browser erişimi için)
# Virgülle ayrılmış origin listesi. Boşsa varsayılan localhost + api-tipbox + minio-tipbox kullanılır.
# MINIO_CORS_ALLOW_ORIGIN=https://api-tipbox.tipbox.co,https://minio-tipbox.tipbox.co,https://app.tipbox.co
