export enum SupportRequestStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  AWAITING_COMPLETION = 'awaiting_completion',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
  REPORTED = 'reported',
}

